// For format details, see https://aka.ms/devcontainer.json. For config options, see the
// README at: 
// https://github.com/devcontainers/templates/tree/main/src/docker-existing-docker-compose
{
	"name": "proc-gen",

	"dockerComposeFile": [
		"../docker-compose.yaml", // Base docker-compose.
		"devcontainer-docker-compose.yaml" // Overrides for devcontainer.
	],

	// Use same service as regular container.
	"service": "pcg",

	// Open /project/ folder by default.
	"workspaceFolder": "/project/",

	// Keep the container running after VS Code shuts down.
	"shutdownAction": "none"
}
